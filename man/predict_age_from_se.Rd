% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioconductor_integration.R
\name{predict_age_from_se}
\alias{predict_age_from_se}
\title{Predict Age from SummarizedExperiment Object}
\usage{
predict_age_from_se(
  se,
  sample_sheet = NULL,
  assay_name = "beta",
  verbose = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment object containing methylation beta values}

\item{sample_sheet}{Optional data frame with sample information. If NULL,
will attempt to extract from colData(se)}

\item{assay_name}{Name of the assay containing beta values (default: "beta")}

\item{verbose}{Logical indicating whether to print progress messages}
}
\value{
Data frame with age predictions
}
\description{
This function demonstrates interoperability with Bioconductor's SummarizedExperiment
class, which is commonly used for storing methylation data from packages like
sesame and minfi.
}
\examples{
\donttest{
# Create example SummarizedExperiment
library(SummarizedExperiment)

# Simulate methylation data
n_probes <- 100
n_samples <- 6
beta_values <- matrix(rbeta(n_probes * n_samples, 2, 2), 
                      nrow = n_probes, ncol = n_samples)
rownames(beta_values) <- paste0("cg", sprintf("\%08d", 1:n_probes))
colnames(beta_values) <- paste0("Sample_", 1:n_samples)

# Create sample metadata
col_data <- data.frame(
  Basename = paste0("Sample_", 1:n_samples),
  Age = c(0.5, 1.2, 2.1, 0.8, 1.5, 2.8),
  Female = c(1, 0, 1, 1, 0, 1),
  Tissue = "Blood"
)

# Create SummarizedExperiment
se <- SummarizedExperiment(
  assays = list(beta = beta_values),
  colData = col_data
)

# Predict ages
results <- predict_age_from_se(se)
}
}
